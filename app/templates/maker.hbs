Maker

<div class="pure-g">
    <div class="pure-u-1-3">
      {{model.nickname}}


      {{#each project in projects}}
        <li>
        {{#if project.title}}
          {{project.title}}
        {{else}}
          Untitled project
        {{/if}}
        </li>
      {{/each}}
    </div>

    <div class="pure-u-2-3">
    {{outlet}}
    </div>
</div>


